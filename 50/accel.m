

function [ax, ay] = accel(x, y, vx, vy)

  ax = 0.0;
  ay = -9.8;

  ## v = sqrt(vx^2+vy^2);

  ## ar_x = - 0.05 * vx * v;
  ## ar_y = - 0.05 * vy * v;

  ## ax = ax + ar_x;
  ## ay = ay + ar_y;

  
